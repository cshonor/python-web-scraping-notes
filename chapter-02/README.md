# 第 2 章 复杂 HTML 解析

## 本章入口

- **完整学习笔记**：[笔记.md](笔记.md)  
- **主示例脚本**：[`complex_parse.py`](complex_parse.py)  

在仓库根目录运行：

```bash
py -3 chapter-02/complex_parse.py
```

或先 `cd chapter-02` 再：`py -3 complex_parse.py`

依赖：`pip install beautifulsoup4 lxml`（`lxml` 可选，解析器可改为 `"lxml"`）。
