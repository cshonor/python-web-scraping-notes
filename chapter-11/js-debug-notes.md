# 接口发现与前端调试（补充）

- **Network / XHR**：筛选 `Fetch/XHR`，记录 URL、方法、Query、请求体与 JSON 响应；可「Copy as cURL」对照复现。
- **内嵌状态**：部分页面在 `<script>` 中写入 `__NEXT_DATA__` 等，可评估是否直接解析（注意版本变化）。
- **合规**：逆向非公开接口可能违反服务条款；优先官方 API 或授权数据。
